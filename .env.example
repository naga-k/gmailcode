# Gmail OAuth — download credentials.json from Google Cloud Console
# Place at: ~/.gmailcode/credentials.json
# First run will open browser for OAuth consent

# Google Cloud Storage (optional — omit to skip cloud sync)
GCS_BUCKET=your-bucket-name

# Sidecar config
SIDECAR_PORT=8765
